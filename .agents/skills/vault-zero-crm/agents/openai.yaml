interface:
  display_name: "Vault Zero Crm"
  short_description: "Repo-specific patterns and workflows for vault-zero-crm"
  default_prompt: "Use the vault-zero-crm repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true